import { NameDirective } from './name.directive';

describe('NameDirective', () => {
  it('should create an instance', () => {
    const directive = new NameDirective();
    expect(directive).toBeTruthy();
  });
});
